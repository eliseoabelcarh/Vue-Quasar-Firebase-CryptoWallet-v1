

import * as types from './types.js'
import * as cryptoService from '../../services/cryptoMarket.js'


export default {
  /*
    [types.SET_USER]: async function ({ commit }) {
      commit(types.SET_USER, await userService.getCurrentUser())
    },
   */
  /* [types.SET_MARKET_PRICES]: async function ({ commit }) {

    commit(types.SET_MARKET_PRICES, { market: 'BTC-ARS', price: await cryptoService.getMarketByCoinsPair('BTC-ARS') })

  } */


}
